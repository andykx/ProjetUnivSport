{% extends 'base.html.twig' %}

{% block body %}
    <div class=" container">

        <h1>Event</h1>

        <table class="table ">
            <tbody>
            <tr>
                 <th>Id</th>
                 <td>{{ event.id }}</td>
            </tr>
            <tr>
                <th>{{'event.titre'|trans}}</th>
                <td>{{ event.titre }}</td>
            </tr>
            <tr>
                <th>{{'event.description'|trans}}</th>
                <td>{{ event.description }}</td>
            </tr>
            </tbody>
        </table>
{% endblock %}